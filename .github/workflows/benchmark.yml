name: Performance Benchmarks

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  benchmark:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.fsproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore dependencies
        run: dotnet restore ./FIO.slnx

      - name: Run benchmarks (DirectRuntime)
        run: |
          dotnet run -c Release --project benchmarks/FIO.Benchmarks -- \
            --direct-runtime \
            --runs 5 \
            --pingpong 50000 \
            --save true \
            --savepath ./benchmark-direct.csv

      - name: Run benchmarks (PollingRuntime)
        run: |
          dotnet run -c Release --project benchmarks/FIO.Benchmarks -- \
            --polling-runtime 4 200 1 \
            --runs 5 \
            --pingpong 50000 \
            --save true \
            --savepath ./benchmark-polling.csv

      - name: Run benchmarks (SignalingRuntime)
        run: |
          dotnet run -c Release --project benchmarks/FIO.Benchmarks -- \
            --signaling-runtime 4 200 1 \
            --runs 5 \
            --pingpong 50000 \
            --save true \
            --savepath ./benchmark-signaling.csv

      - name: Display benchmark summary
        run: |
          echo "## Benchmark Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### DirectRuntime" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          cat ./benchmark-direct.csv >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### PollingRuntime" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          cat ./benchmark-polling.csv >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### SignalingRuntime" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          cat ./benchmark-signaling.csv >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY

      - name: Convert results to benchmark JSON
        run: |
          # Extract average execution times from each runtime's CSV and create JSON for tracking
          # Format: [{ "name": "Runtime - Benchmark", "unit": "ms", "value": avg_time }]
          echo "[" > benchmark-results.json

          # DirectRuntime
          direct_avg=$(awk -F',' 'NR==2 {gsub(/[[:space:]]/, "", $4); print $4}' ./benchmark-direct.csv)
          echo "  { \"name\": \"DirectRuntime - Pingpong\", \"unit\": \"ms\", \"value\": $direct_avg }," >> benchmark-results.json

          # PollingRuntime
          polling_avg=$(awk -F',' 'NR==2 {gsub(/[[:space:]]/, "", $4); print $4}' ./benchmark-polling.csv)
          echo "  { \"name\": \"PollingRuntime - Pingpong\", \"unit\": \"ms\", \"value\": $polling_avg }," >> benchmark-results.json

          # SignalingRuntime
          signaling_avg=$(awk -F',' 'NR==2 {gsub(/[[:space:]]/, "", $4); print $4}' ./benchmark-signaling.csv)
          echo "  { \"name\": \"SignalingRuntime - Pingpong\", \"unit\": \"ms\", \"value\": $signaling_avg }" >> benchmark-results.json

          echo "]" >> benchmark-results.json

          echo "### Benchmark JSON for tracking:" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`json" >> $GITHUB_STEP_SUMMARY
          cat benchmark-results.json >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY

      - name: Store benchmark results
        uses: benchmark-action/github-action-benchmark@v1
        with:
          tool: 'customSmallerIsBetter'
          output-file-path: benchmark-results.json
          github-token: ${{ secrets.GITHUB_TOKEN }}
          auto-push: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
          comment-on-alert: true
          alert-threshold: '150%'
          fail-on-alert: false
          alert-comment-cc-users: '@${{ github.actor }}'

      - name: Upload benchmark results
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-results
          path: |
            ./benchmark-*.csv
            ./benchmark-results.json
